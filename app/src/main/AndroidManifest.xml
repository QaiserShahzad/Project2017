<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qaiserpasha.project2017">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".SighUpFolder.DonarSignUp" />
        <activity android:name=".SighUpFolder.DoneeSignUp" />
        <activity android:name=".SighUpFolder.SignUpOption" />
        <activity android:name=".WelcomeDonar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".StartMap" />
        <activity android:name=".SighUpFolder.CollectorSignUp" />
        <activity
            android:name=".WelcomeCollector"
            android:label="@string/title_activity_welcome_collector"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonarMap"
            android:label="@string/title_activity_donar_map">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeDonee"
            android:label="@string/title_activity_welcome_recipient"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SighUpFolder.AdminSignUp" />
        <activity
            android:name=".WelcomeAdmin"
            android:label="@string/title_activity_welcome_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DonorApprovedRequest" />
        <activity android:name=".Collecter_Info_gathering" />
        <activity android:name=".DoneeSearch" />
        <activity android:name=".AdminSignUpOption" />
        <activity android:name=".Donee_Info_Gathering" />
        <activity android:name=".ShowReviews" />
        <activity android:name=".MaxDonationn"></activity>
    </application>

</manifest>